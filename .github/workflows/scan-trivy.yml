- name: Run Trivy scan (action)
  uses: aquasecurity/trivy-action@v0.33.1   # <-- pin to an existing release
  with:
    image-ref: my-org/my-app:${{ github.sha }}
    format: json
    output: trivy-report.json
    severity: CRITICAL,HIGH,MEDIUM
